{
  "name": "@arribada/plane-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "Browser harness for validating the Arribada Plane fork against a running deployment.",
  "type": "module",
  "scripts": {
    "auth": "node bin/capture-auth.mjs",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "1.49.1"
  }
}
